import { Config, DOMPurifyI, HookEvent, HookName } from 'dompurify';
export { Config, DOMPurifyI, HookEvent, HookName };
export declare type HookCallback = (currentNode: Element, data: HookEvent, config: Config) => void;
export declare type Hooks = Map<HookName, HookCallback>;
export declare type NormalizedHref = {
    requestedUrl: string;
    normalizedUrl: string;
    requestedFragment: string;
    normalizedFragment: string;
    protocol: string;
};
//# sourceMappingURL=types.d.ts.map