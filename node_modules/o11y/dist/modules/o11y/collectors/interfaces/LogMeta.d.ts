import { SchematizedPayload } from './SchematizedPayload';
export interface LogMeta {
    timestamp: number;
    rootId: string;
    sequence: number;
    loggerName: string;
    pagePayload: SchematizedPayload;
    loggerAppName: string;
    connectionType: string;
    appPayload: SchematizedPayload;
}
