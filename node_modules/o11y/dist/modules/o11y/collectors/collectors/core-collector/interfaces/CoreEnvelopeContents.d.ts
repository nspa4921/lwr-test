import { UpCounter } from 'src/collectors/metrics/UpCounter';
import { ValueRecorder } from 'src/collectors/metrics/ValueRecorder';
import { Schema } from 'src/interfaces';
import { LogMessage } from './LogMessage';
import { StaticAttributes } from './StaticAttributes';
export interface CoreEnvelopeContents {
    messages?: Map<Schema, LogMessage[]>;
    upCounters?: UpCounter[];
    valueRecorders?: ValueRecorder[];
    staticAttributes: StaticAttributes;
}
